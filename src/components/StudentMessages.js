import React from 'react'

function StudentMessages() {
  return (
    <div>

    <h1>Create a Messages page</h1>
    <h2>Assigned into Archana</h2>

    <ul>
        <li>Receive data from server and display in table format</li>
      </ul>
    
  </div>
  )
}

export default StudentMessages