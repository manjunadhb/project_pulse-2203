import React from 'react'

function DailyStatusUpdate() {
  return (
    <div>

    <h1>Create a Daily Status Update page</h1>
    <h2>Assigned into Srikanth K</h2>

    <ul>
        <li>Form with validation</li>
        <li>Send form values to server</li>
    </ul>

    
  </div>
  )
}

export default DailyStatusUpdate